---
- name: Install base applications
  apt:
    name: "{{ packages }}"
    state: present
  vars:
    packages:
      - curl
      - vim
      - zsh
      - git
      - mc
      - build-essential
      - file
      - tmux
      - imagemagick
      - apt-transport-https
